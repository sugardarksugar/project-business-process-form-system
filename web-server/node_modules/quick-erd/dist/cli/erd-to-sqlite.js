#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable no-console */
const text_to_sqlite_1 = require("../db/text-to-sqlite");
function main() {
    let text = '';
    process.stdin
        .on('data', chunk => (text += chunk))
        .on('end', () => {
        if (!text) {
            console.error('missing erd text from stdin');
            process.exit(1);
        }
        const { up, down } = text_to_sqlite_1.textToSqlite(text);
        console.log('-- Up');
        console.log(up);
        console.log();
        console.log('-- Down');
        console.log(down);
    });
}
main();
